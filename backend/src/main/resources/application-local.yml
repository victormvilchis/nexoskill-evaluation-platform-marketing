spring:
  config:
    activate:
      on-profile: local

management:
  health:
    mail:
      enabled: false

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5174}
  security:
    trust-forwarded-headers: false
