ALTER TABLE MKT_PROSPECT ADD (
    UTM_SOURCE VARCHAR2(100 CHAR),
    UTM_MEDIUM VARCHAR2(100 CHAR),
    UTM_CAMPAIGN VARCHAR2(160 CHAR),
    UTM_CONTENT VARCHAR2(160 CHAR),
    UTM_TERM VARCHAR2(160 CHAR),
    CLICK_ID VARCHAR2(200 CHAR),
    CLICK_ID_TYPE VARCHAR2(30 CHAR),
    REFERRER_URL VARCHAR2(500 CHAR),
    LANDING_PAGE VARCHAR2(500 CHAR),
    CONVERSION_PAGE VARCHAR2(500 CHAR),
    ATTRIBUTION_CAPTURED_AT TIMESTAMP WITH TIME ZONE,
    ANALYTICS_CONSENT VARCHAR2(20 CHAR) DEFAULT 'UNSET' NOT NULL
);

ALTER TABLE MKT_PROSPECT ADD CONSTRAINT CK_MKT_PROSPECT_ANALYTICS
    CHECK (ANALYTICS_CONSENT IN ('GRANTED', 'DENIED', 'UNSET'));

CREATE INDEX IX_MKT_PROSPECT_CAMPAIGN_CREATED
    ON MKT_PROSPECT (UTM_SOURCE, UTM_CAMPAIGN, CREATED_AT);
